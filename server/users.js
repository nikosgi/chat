const users = []
const rooms = [{
  id: "1", space: 2, active: 0
}]

export const addUser = user => {
  users.push(user)
  if( )
}

export const removeUser = user => {
  users.filter( u => u !== user)
}